# Override para testar o stack de produção localmente (sem Traefik/Coolify).
# Uso: make prod-local  (ou docker compose -f docker-compose.yml -f docker-compose.local.yml up)
services:
  frontend:
    ports:
      - "9999:80"
